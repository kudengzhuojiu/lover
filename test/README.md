# To My Lovely Vũ

一个无需构建、可直接部署的移动端表白 H5。

## 本地预览

直接双击 `index.html`，或使用任意静态服务器打开本目录。

## 个性化

- 在 `script.js` 修改 `startDate` 为你们认识的日期。
- 在 `index.html` 的三张照片卡片替换为你们的图片（可加在 `assets/` 文件夹）。
- 将你有使用权的歌曲命名为 `love.mp3` 放在项目根目录，即可启用右上角音乐按钮。

## 部署

推送至 GitHub 后，可在 Vercel 或 GitHub Pages 选择此仓库直接部署。
